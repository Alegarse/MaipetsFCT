<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.maipetsfct">
    <!-- Para poder usar internet -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Para poder usar la cámara -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" /> <!-- Para poder escribir y leer en el dispositivo -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PopServ"
            android:theme="@style/AppTheme.PopUp"/>
        <activity android:name=".AddServActivity" />
        <activity android:name=".ServActivity" />
        <activity android:name=".VetActivity" />
        <activity
            android:name=".PopPet"
            android:theme="@style/AppTheme.PopUp" />
        <activity android:name=".AddMascActivity" />
        <activity
            android:name=".PopUpSelect"
            android:theme="@style/AppTheme.PopUp" />
        <activity android:name=".UsersActivity" />
        <activity android:name=".AnotherServRegister" />
        <activity android:name=".VetRegister" />
        <activity android:name=".FamilyRegister" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".NewUserActivity" />
        <activity android:name=".PostMainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>